import 'package:flutter/material.dart';

String resultDataNullStringFor({required String request}) =>
    "result.data was null for $request";

BorderRadius get defaultRadius => const BorderRadius.all(
      Radius.circular(15),
    );
