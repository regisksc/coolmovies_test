name: Flutter CI

on: [push]

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: regisksc/coolmovies-test/.github/actions/analyze@main

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: regisksc/coolmovies-test/.github/actions/test@main
